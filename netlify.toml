[build]
  command = "npm run build"
  publish = "dist"

# Redirect for Single Page Apps (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Proxy /api calls to Netlify Functions (if you decide to use them)
# [[redirects]]
#   from = "/api/*"
#   to = "/.netlify/functions/:splat"
#   status = 200
