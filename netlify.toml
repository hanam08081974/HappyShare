[build]
  command = "npm run build"
  publish = "dist"

# Redirect for API calls to Netlify Functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Redirect for Single Page Apps (SPA) fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
