[build]
  command = "npm run build"
  publish = "dist"

# Redirect for API function
[[redirects]]
  from = "/api/invite"
  to = "/.netlify/functions/invite"
  status = 200

# Redirect for Single Page Apps (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
